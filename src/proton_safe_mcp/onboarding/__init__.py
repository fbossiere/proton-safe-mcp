"""Setup-assistant service layer.

This package orchestrates the managed installation. It is deliberately free of any Qt
import so the whole flow can be tested, and so the MCP server keeps working without Qt.
"""
